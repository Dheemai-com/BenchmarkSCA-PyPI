# BenchmarkSCA-PyPI

> **Warning:** this repository intentionally pins vulnerable package examples for scanner
> evaluation. Do not install or execute its dependencies.

A PyPI SCA accuracy corpus for Vybscan. `ground-truth.json` is the independent vulnerable/clean
oracle. This repository remains internal until all labels are freshly reverified and transitive and
constraint-versus-lockfile cases are expanded.
